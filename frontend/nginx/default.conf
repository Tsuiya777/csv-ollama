server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # APIリクエストはFastAPIへプロキシ
    location /api/ {
        proxy_pass http://fastapi:8000/;
    }
}
